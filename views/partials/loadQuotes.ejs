<!-- a loop thats shows all Quotes the user has created-->
<ul>
    <% Quotes.forEach(function(Quote) { %>
    <form action="/deleteQuote" method="post">
        <div class="Quotes">
          <div class="Quote" id="Post1">
            <input type ="hidden" name = "name" value="<%= Quote.ProjectName %>">
            <div class="QuoteTitle"> <%= Quote.ProjectName %></div>
            <div class="QuoteDuration"> Duration: <%= Quote.Days %> days</div>
            <div class="QuoteDev"> Developer: <%= Quote.Developer %></div>
            <div class="QuoteCost"> Cost: <%= Quote.FinalBudget %></div>
            <button class="Deletebutton" type="submit">Delete</button> 
          </div>
        </div>
    </form>
    <% }); %>
  </ul>